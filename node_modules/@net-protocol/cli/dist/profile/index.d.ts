import { Command } from 'commander';

/**
 * Register the profile command with the commander program
 */
declare function registerProfileCommand(program: Command): void;

export { registerProfileCommand };
